apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "redhat-sso.fullname" . }}-test-connection"
  labels:
    {{- include "redhat-sso.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "redhat-sso.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
